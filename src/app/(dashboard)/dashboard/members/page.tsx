import { PageShell } from "@/components/layout/page-shell";

export default function MembersPage() {
  return (
    <PageShell
      eyebrow="Dashboard"
      title="멤버 관리"
      description="워크스페이스 초대, 역할 변경, 접근 권한을 관리합니다."
    />
  );
}

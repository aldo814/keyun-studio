import { PageShell } from "@/components/layout/page-shell";

export default function DashboardPage() {
  return (
    <PageShell
      eyebrow="Dashboard"
      title="사용자 대시보드"
      description="내 사이트, 최근 작업, 구독 상태를 한 번에 보는 고객용 홈입니다."
    />
  );
}
